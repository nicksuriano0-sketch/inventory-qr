<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Inventory Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f5f5f5;
        }
        h1 {
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        th, td {
            padding: 12px;
            border-bottom: 1px solid #ddd;
            text-align: center;
        }
        th {
            background-color: #4CAF50;
            color: white;
        }
        img.qr {
            width: 60px;
            height: 60px;
        }
        form {
            display: inline;
        }
        button {
            padding: 5px 10px;
            margin: 2px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button.plus { background-color: #4CAF50; color: white; }
        button.minus { background-color: #f44336; color: white; }
        button.edit { background-color: #2196F3; color: white; }
        button.delete { background-color: #ff9800; color: white; }
        a.logout {
            float: right;
            margin-top: -40px;
            text-decoration: none;
            padding: 8px 15px;
            background-color: #f44336;
            color: white;
            border-radius: 4px;
        }
        a.add-item {
            display: inline-block;
            margin-top: 20px;
            text-decoration: none;
            padding: 8px 15px;
            background-color: #4CAF50;
            color: white;
            border-radius: 4px;
        }
    </style>
</head>
<body>

<h1>Inventory Dashboard</h1>

<a href="{{ url_for('logout') }}" class="logout">Logout</a>
<a href="{{ url_for('add_item_route') }}" class="add-item">Add New Item</a>

<table>
    <tr>
        <th>QR Code</th>
        <th>Name</th>
        <th>Category</th>
        <th>Quantity</th>
        <th>Actions</th>
    </tr>
    {% for item in items %}
    <tr>
        <td><img class="qr" src="data:image/png;base64,{{ item.qr_base64 }}" alt="QR Code"></td>
        <td>{{ item.name }}</td>
        <td>{{ item.category }}</td>
        <td>{{ item.quantity }}</td>
        <td>
            <!-- Quantity update buttons -->
            <form method="POST" action="{{ url_for('update_quantity_route', item_id=item.id) }}">
                <input type="hidden" name="delta" value="1">
                <button type="submit" class="plus">+</button>
            </form>
            <form method="POST" action="{{ url_for('update_quantity_route', item_id=item.id) }}">
                <input type="hidden" name="delta" value="-1">
                <button type="submit" class="minus">-</button>
            </form>
            <!-- Edit / Delete -->
            <a href="{{ url_for('edit_item_route', item_id=item.id) }}"><button class="edit">Edit</button></a>
            <a href="{{ url_for('delete_item_route', item_id=item.id) }}"><button class="delete">Delete</button></a>
        </td>
    </tr>
    {% endfor %}
</table>

</body>
</html>
